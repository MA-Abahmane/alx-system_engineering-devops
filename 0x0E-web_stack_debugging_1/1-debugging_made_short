#!/usr/bin/env bash
file="$1"
sed -i -E -e 's/listen [0-9]* default_server/listen 80 default_server/g' -e 's/listen \[::\]:[0-9]* default_server/listen \[::\]:80 default_server/g' "$file"
sudo service nginx restart
kill "$(pgrep 'nginx' | head -1)"
